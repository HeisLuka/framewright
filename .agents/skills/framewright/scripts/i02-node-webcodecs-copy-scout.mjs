#!/usr/bin/env node
import fs from 'node:fs';
import fsp from 'node:fs/promises';
import path from 'node:path';
import vm from 'node:vm';
import os from 'node:os';
import {spawn} from 'node:child_process';
import {performance} from 'node:perf_hooks';
import {createCanvas, Image, GlobalFonts} from '@napi-rs/canvas';
import {
  VideoEncoder, VideoFrame, EncodedVideoChunk,
  AudioEncoder, AudioData, EncodedAudioChunk,
  Mp4Muxer,
} from '@napi-rs/webcodecs';

const root=process.cwd();
const htmlPath=path.resolve(process.env.HTML||'examples/book-ad-systems/index-e18-for-i02.html');
const manifestPath=path.resolve(process.env.MANIFEST||'examples/book-ad-systems/generated-i02/manifest.json');
const outDir=path.resolve(process.env.OUT_DIR||'artifacts/i02-copy-native');
const reportPath=path.resolve(process.env.COPY_REPORT||path.join(outDir,'report.json'));
const summaryPath=path.resolve(process.env.COPY_SUMMARY||path.join(outDir,'summary.md'));
const trackPath=path.resolve(process.env.TRACK||'artifacts/i02/track.wav');
const repeats=Math.max(1,Math.min(10,Math.trunc(Number(process.env.COPY_REPEATS||3))));
const bitrate=Math.max(250000,Math.trunc(Number(process.env.COPY_BITRATE||2000000)));
const audioBitrate=Math.max(32000,Math.trunc(Number(process.env.COPY_AUDIO_BITRATE||192000)));
const codec=process.env.COPY_CODEC||'avc1.420028';
const selector={bookId:process.env.BOOK_ID||'river-station',variant:process.env.VARIANT||'hook-first',profile:process.env.PROFILE||'vertical'};

for(const[file,family]of[['/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf','DejaVu Sans'],['/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf','DejaVu Sans']]){
  if(!GlobalFonts.registerFromPath(file,family))throw new Error(`failed to register ${file}`);
}
for(const file of[htmlPath,manifestPath,trackPath])if(!fs.existsSync(file))throw new Error(`missing ${file}`);
await fsp.mkdir(outDir,{recursive:true});
const manifest=JSON.parse(await fsp.readFile(manifestPath,'utf8'));
const entry=manifest.items.find(x=>x.bookId===selector.bookId&&x.variant===selector.variant&&x.profile===selector.profile);
if(!entry)throw new Error(`fixture not found ${JSON.stringify(selector)}`);
const payload=JSON.parse(await fsp.readFile(path.resolve(path.dirname(manifestPath),entry.payloadFile),'utf8'));
if(payload.cover_url&&!/^[a-z]+:/i.test(payload.cover_url)&&!path.isAbsolute(payload.cover_url))payload.cover_url=path.resolve(path.dirname(htmlPath),payload.cover_url);
const html=await fsp.readFile(htmlPath,'utf8'),match=html.match(/<script>([\s\S]*?)<\/script>/i);if(!match)throw new Error('inline scene script not found');
const sceneSource=match[1],yieldTurn=()=>new Promise(r=>setImmediate(r));

function stat(xs){const v=[...xs].filter(Number.isFinite).sort((a,b)=>a-b);if(!v.length)return{mean:0,p50:0,p95:0,min:0,max:0};const q=p=>v[Math.min(v.length-1,Math.floor((v.length-1)*p))],mean=v.reduce((a,b)=>a+b,0)/v.length;return{mean:+mean.toFixed(3),p50:+q(.5).toFixed(3),p95:+q(.95).toFixed(3),min:+q(0).toFixed(3),max:+q(1).toFixed(3)};}
function procTreeRssBytes(rootPid=process.pid){try{const pids=fs.readdirSync('/proc').filter(x=>/^\d+$/.test(x)).map(Number),parent=new Map(),rss=new Map();for(const pid of pids){try{const s=fs.readFileSync(`/proc/${pid}/status`,'utf8'),pp=s.match(/^PPid:\s+(\d+)/m),rr=s.match(/^VmRSS:\s+(\d+)\s+kB/m);if(pp)parent.set(pid,Number(pp[1]));if(rr)rss.set(pid,Number(rr[1])*1024);}catch{}}const wanted=new Set([rootPid]);let changed=true;while(changed){changed=false;for(const[pid,ppid]of parent)if(wanted.has(ppid)&&!wanted.has(pid)){wanted.add(pid);changed=true;}}let total=0;for(const pid of wanted)total+=rss.get(pid)||0;return total;}catch{return process.memoryUsage().rss;}}
function cgroupCpuUsec(){try{const m=fs.readFileSync('/sys/fs/cgroup/cpu.stat','utf8').match(/^usage_usec\s+(\d+)/m);return m?Number(m[1]):null;}catch{return null;}}
function startResources(){let peak=procTreeRssBytes();const cpu0=cgroupCpuUsec();const timer=setInterval(()=>peak=Math.max(peak,procTreeRssBytes()),20);timer.unref?.();return()=>{clearInterval(timer);peak=Math.max(peak,procTreeRssBytes());const cpu1=cgroupCpuUsec();return{peakProcessTreeRssBytes:peak,cgroupCpuMs:cpu0!=null&&cpu1!=null?+((cpu1-cpu0)/1000).toFixed(3):null};};}
function run(command,args){return new Promise((resolve,reject)=>{const child=spawn(command,args,{cwd:root,stdio:['ignore','pipe','pipe']});let stdout='',stderr='';child.stdout.on('data',d=>stdout+=d);child.stderr.on('data',d=>stderr+=d);child.once('error',reject);child.once('close',(code,signal)=>code===0?resolve({stdout,stderr}):reject(new Error(`${command} exited ${code??signal}\n${stderr.slice(-5000)}`)));});}
async function ffprobe(file){const{stdout}=await run('ffprobe',['-v','error','-count_frames','-show_entries','format=duration,size:stream=index,codec_type,codec_name,width,height,avg_frame_rate,nb_read_frames,duration,sample_rate,channels','-of','json',file]);return JSON.parse(stdout);}
async function metric(kind,a,b){const filter=kind==='ssim'?'[0:v][1:v]ssim':'[0:v][1:v]psnr';const{stderr}=await run('ffmpeg',['-hide_banner','-loglevel','info','-i',a,'-i',b,'-lavfi',filter,'-an','-f','null','-']);if(kind==='ssim')return Number([...stderr.matchAll(/All:([0-9.]+)/g)].at(-1)?.[1]||NaN);return Number([...stderr.matchAll(/average:([0-9.]+)/g)].at(-1)?.[1]||NaN);}
async function compareVideo(a,b){return{ssim:await metric('ssim',a,b),psnr:await metric('psnr',a,b)};}
async function nodeContext(){const canvas=createCanvas(entry.width,entry.height),document={createElement(n){if(String(n).toLowerCase()!=='canvas')throw new Error(`unsupported ${n}`);return createCanvas(1,1);},getElementById(id){return id==='c'?canvas:null;}},window={FRAMEWRIGHT_PAYLOAD:structuredClone(payload)},location={search:`?f=0&w=${entry.width}&h=${entry.height}&s=${entry.seed}&profile=${entry.profile}`},sandbox={window,document,Image,URLSearchParams,location,console,performance,setTimeout,clearTimeout,requestAnimationFrame(){return 0;}};sandbox.globalThis=sandbox;window.window=window;window.document=document;window.location=location;const ctx=vm.createContext(sandbox),t0=performance.now();vm.runInContext(sceneSource,ctx,{filename:htmlPath});const deadline=performance.now()+10000;while(!window.__ready&&performance.now()<deadline)await new Promise(r=>setTimeout(r,10));if(!window.__ready||window.__bootError)throw new Error(window.__bootError||'boot timeout');vm.runInContext(`globalThis.__render=(n,w,s)=>{renderFrame(n,w,s,document.getElementById('c'));return document.getElementById('c');};`,ctx);return{ctx,window,canvas,initMs:performance.now()-t0};}
function parseWav(file){const buf=fs.readFileSync(file);if(buf.toString('ascii',0,4)!=='RIFF'||buf.toString('ascii',8,12)!=='WAVE')throw new Error('not RIFF/WAVE');let off=12,fmt=null,data=null;while(off+8<=buf.length){const id=buf.toString('ascii',off,off+4),size=buf.readUInt32LE(off+4),start=off+8;if(id==='fmt ')fmt={format:buf.readUInt16LE(start),channels:buf.readUInt16LE(start+2),sampleRate:buf.readUInt32LE(start+4),blockAlign:buf.readUInt16LE(start+12),bitsPerSample:buf.readUInt16LE(start+14)};else if(id==='data')data=buf.subarray(start,Math.min(buf.length,start+size));off=start+size+(size%2);}if(!fmt||!data||fmt.format!==1||fmt.bitsPerSample!==16)throw new Error('expected PCM16 WAV');return{...fmt,frames:Math.floor(data.length/fmt.blockAlign),data};}
function copyChunk(chunk){const data=new Uint8Array(chunk.byteLength);chunk.copyTo(data);const out={type:chunk.type,timestamp:chunk.timestamp,data};if(chunk.duration!=null)out.duration=chunk.duration;return out;}
function copyDesc(metadata){const d=metadata?.decoderConfig?.description;return d?new Uint8Array(d):null;}
function rebuildVideo(p){return new EncodedVideoChunk(p);}
function rebuildAudio(p){return new EncodedAudioChunk(p);}

async function renderOne(runIndex){const stop=startResources(),wall0=performance.now(),{ctx,window,canvas,initMs}=await nodeContext(),total=Number(window.RISO.total),fps=Number(window.RISO.fps),frameDur=Math.trunc(1e6/fps),wav=parseWav(trackPath),output=path.join(outDir,`copy-native-${runIndex}.mp4`);let videoError=null,audioError=null,videoDesc=null,audioDesc=null,maxQueue=0;const videoPackets=[],audioPackets=[],drawMs=[],extractMs=[],yieldMs=[];
  const ve=new VideoEncoder({output(chunk,metadata){videoPackets.push(copyChunk(chunk));if(!videoDesc)videoDesc=copyDesc(metadata);},error(e){videoError=e;}});ve.configure({codec,width:canvas.width,height:canvas.height,bitrate,framerate:fps,latencyMode:'realtime',hardwareAcceleration:'prefer-software'});
  const video0=performance.now();for(let frame=0;frame<total;frame++){const d0=performance.now(),rendered=ctx.__render(frame,entry.width,entry.seed);drawMs.push(performance.now()-d0);const e0=performance.now();{const rgba=rendered.getContext('2d').getImageData(0,0,rendered.width,rendered.height).data,bytes=new Uint8Array(rgba.buffer,rgba.byteOffset,rgba.byteLength),vf=new VideoFrame(bytes,{format:'RGBA',codedWidth:rendered.width,codedHeight:rendered.height,timestamp:Math.trunc(frame*1e6/fps),duration:frameDur});ve.encode(vf,{keyFrame:frame===0||frame%Math.max(1,Math.round(fps*2))===0});vf.close();}extractMs.push(performance.now()-e0);maxQueue=Math.max(maxQueue,ve.encodeQueueSize);const y0=performance.now();await yieldTurn();yieldMs.push(performance.now()-y0);if(videoError)throw videoError;}await ve.flush();if(videoError)throw videoError;const videoMs=performance.now()-video0;ve.close();
  const ae=new AudioEncoder({output(chunk,metadata){audioPackets.push(copyChunk(chunk));if(!audioDesc)audioDesc=copyDesc(metadata);},error(e){audioError=e;}});ae.configure({codec:'mp4a.40.2',sampleRate:wav.sampleRate,numberOfChannels:wav.channels,bitrate:audioBitrate});const audio0=performance.now(),block=1024;for(let startFrame=0;startFrame<wav.frames;startFrame+=block){const frames=Math.min(block,wav.frames-startFrame),a=startFrame*wav.blockAlign,b=a+frames*wav.blockAlign,ad=new AudioData({format:'s16',sampleRate:wav.sampleRate,numberOfFrames:frames,numberOfChannels:wav.channels,timestamp:Math.trunc(startFrame*1e6/wav.sampleRate),data:new Uint8Array(wav.data.subarray(a,b))});ae.encode(ad);ad.close();if((startFrame/block)%32===31)await yieldTurn();if(audioError)throw audioError;}await ae.flush();if(audioError)throw audioError;const audioMs=performance.now()-audio0;ae.close();if(!videoDesc?.length||!audioDesc?.length)throw new Error('missing codec descriptions');
  const mux0=performance.now(),mux=new Mp4Muxer();mux.addVideoTrack({codec,width:canvas.width,height:canvas.height,framerate:fps,description:videoDesc});mux.addAudioTrack({codec:'mp4a.40.2',sampleRate:wav.sampleRate,numberOfChannels:wav.channels,description:audioDesc});for(const p of videoPackets)mux.addVideoChunk(rebuildVideo(p));for(const p of audioPackets)mux.addAudioChunk(rebuildAudio(p));mux.flush();const mp4=mux.finalize();mux.close();const muxMs=performance.now()-mux0,w0=performance.now();await fsp.writeFile(output,mp4);const writeMs=performance.now()-w0,wallMs=performance.now()-wall0,resources=stop();await run('ffmpeg',['-hide_banner','-loglevel','error','-i',output,'-map','0:v:0','-an','-f','null','-']);await run('ffmpeg',['-hide_banner','-loglevel','error','-i',output,'-map','0:a:0','-vn','-f','null','-']);return{backend:'node-canvas-rgba-copy-webcodecs',runIndex,output,wallMs:+wallMs.toFixed(3),initMs:+initMs.toFixed(3),videoMs:+videoMs.toFixed(3),audioMs:+audioMs.toFixed(3),muxMs:+muxMs.toFixed(3),writeMs:+writeMs.toFixed(3),drawMs:stat(drawMs),extractMs:stat(extractMs),yieldMs:stat(yieldMs),maxQueue,videoChunks:videoPackets.length,audioChunks:audioPackets.length,encodedVideoBytes:videoPackets.reduce((a,p)=>a+p.data.byteLength,0),encodedAudioBytes:audioPackets.reduce((a,p)=>a+p.data.byteLength,0),outputBytes:mp4.byteLength,resources,probe:await ffprobe(output)};}
async function ownRaster(output,total,fps){const frames=[Math.round(fps),Math.round(total*.32),Math.round(total*.61),Math.min(total-1,Math.round(total*.9))],dir=path.join(outDir,'quality');await fsp.mkdir(dir,{recursive:true});const node=await nodeContext(),rows=[];for(const frame of frames){const ref=path.join(dir,`ref-${frame}.png`),dec=path.join(dir,`decoded-${frame}.png`);await fsp.writeFile(ref,node.ctx.__render(frame,entry.width,entry.seed).toBuffer('image/png'));await run('ffmpeg',['-hide_banner','-loglevel','error','-y','-i',output,'-map','0:v:0','-an','-vf',`select=eq(n\\,${frame})`,'-vsync','vfr','-frames:v','1',dec]);rows.push({frame,...await compareVideo(ref,dec)});}return rows;}

console.log(`I02 copy-native scout: ${entry.id}`);const runs=[];for(let i=1;i<=repeats;i++){console.log(`copy-native ${i}/${repeats}`);runs.push(await renderOne(i));}const stream=runs[0].probe.streams.find(s=>s.codec_type==='video'),totalFrames=Number(stream?.nb_read_frames||entry.frames||360),fps=Number(String(stream?.avg_frame_rate||'30/1').split('/')[0])||30,samples=await ownRaster(runs[0].output,totalFrames,fps),quality={samples,meanSsim:+(samples.reduce((a,x)=>a+x.ssim,0)/samples.length).toFixed(6),worstSsim:+Math.min(...samples.map(x=>x.ssim)).toFixed(6),meanPsnr:+(samples.reduce((a,x)=>a+x.psnr,0)/samples.length).toFixed(4),worstPsnr:+Math.min(...samples.map(x=>x.psnr)).toFixed(4)},summary={wallMs:stat(runs.map(x=>x.wallMs)),videoMs:stat(runs.map(x=>x.videoMs)),audioMs:stat(runs.map(x=>x.audioMs)),muxMs:stat(runs.map(x=>x.muxMs)),outputBytes:stat(runs.map(x=>x.outputBytes)),peakRssBytes:stat(runs.map(x=>x.resources.peakProcessTreeRssBytes)),cgroupCpuMs:stat(runs.map(x=>x.resources.cgroupCpuMs).filter(x=>x!=null)),videosPerHourFromMeanWall:+(3600000/(runs.reduce((a,x)=>a+x.wallMs,0)/runs.length)).toFixed(2)};
const x264Ref=path.resolve(process.env.X264_REF||'artifacts/i02/node-crf-1.mp4'),webRef=path.resolve(process.env.WEB_REF||'artifacts/i02/web-1.mp4');const report={schema:'framewright-i02-node-webcodecs-copy-scout-v1',warning:'Scout only. Compressed native chunk wrappers are copied to plain Uint8Array immediately; mux reconstructs chunks after encoding. v1.3.0 fastStart remains disabled.',fixture:{...selector,id:entry.id,style:entry.style,width:entry.width,height:entry.height,seed:entry.seed},config:{repeats,codec,bitrate,audioBitrate,package:'@napi-rs/webcodecs@1.3.0',frameSource:'getImageData -> Uint8Array -> VideoFrame(buffer)',eventLoopYieldEachFrame:true,packetRetention:'copy bytes, release native wrapper',mp4FastStart:false},host:{platform:process.platform,arch:process.arch,node:process.version,cpus:os.cpus().length,cpuModel:os.cpus()[0]?.model||null,totalMemoryBytes:os.totalmem()},summary,quality:{ownRaster:quality,copyVsX264Crf:fs.existsSync(x264Ref)?await compareVideo(runs[0].output,x264Ref):null,copyVsChromium:fs.existsSync(webRef)?await compareVideo(runs[0].output,webRef):null},runs};await fsp.writeFile(reportPath,`${JSON.stringify(report,null,2)}\n`);const mb=n=>(n/1048576).toFixed(2),s=summary,md=`# I02 packet-copy native WebCodecs scout\n\n| metric | copy-native |\n|---|---:|\n| mean wall | ${(s.wallMs.mean/1000).toFixed(3)} s |\n| videos/hour | ${s.videosPerHourFromMeanWall} |\n| mean CPU | ${(s.cgroupCpuMs.mean/1000).toFixed(3)} s |\n| peak RSS | ${mb(s.peakRssBytes.max)} MiB |\n| mean MP4 | ${mb(s.outputBytes.mean)} MiB |\n| mean video stage | ${(s.videoMs.mean/1000).toFixed(3)} s |\n| mean AAC stage | ${(s.audioMs.mean/1000).toFixed(3)} s |\n| mean native mux | ${s.muxMs.mean.toFixed(1)} ms |\n| own-raster mean/worst SSIM | ${quality.meanSsim} / ${quality.worstSsim} |\n| own-raster mean/worst PSNR | ${quality.meanPsnr} / ${quality.worstPsnr} dB |\n\nHard gate: both video and audio streams decode independently. Native encoded chunk wrappers are not retained across the job.\n`;await fsp.writeFile(summaryPath,md);console.log(md);
