#!/usr/bin/env node
import fs from 'node:fs';
import fsp from 'node:fs/promises';
import http from 'node:http';
import path from 'node:path';
import vm from 'node:vm';
import { performance } from 'node:perf_hooks';
import puppeteer from 'puppeteer';
import { createCanvas, Image, GlobalFonts } from '@napi-rs/canvas';
import { VideoEncoder as NativeVideoEncoder, VideoFrame as NativeVideoFrame } from '@napi-rs/webcodecs';

const root = process.cwd();
const htmlPath = path.resolve(process.env.HTML || 'examples/book-ad-systems/index-e18-for-r38.html');
const manifestPath = path.resolve(process.env.MANIFEST || 'examples/book-ad-systems/generated-r38/manifest.json');
const outDir = path.resolve(process.env.OUT_DIR || 'artifacts/r38');
const reportPath = path.resolve(process.env.REPORT || path.join(outDir, 'report.json'));
const jobsPerScenario = Math.max(4, Math.min(24, Number(process.env.JOBS || 8)));
const bitrate = Number(process.env.BITRATE || 3_000_000);
const queueLimit = 8;
const concurrencies = String(process.env.CONCURRENCY || '1,2,3,4').split(',').map(Number).filter(x => [1,2,3,4].includes(x));
const selector = { bookId: 'river-station', variant: 'hook-first', profile: 'vertical' };

await fsp.mkdir(outDir, { recursive: true });
for (const [file, family] of [['/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf','DejaVu Sans'],['/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf','DejaVu Sans']]) GlobalFonts.registerFromPath(file, family);
for (const file of [htmlPath, manifestPath]) if (!fs.existsSync(file)) throw new Error(`missing ${file}`);
const manifest = JSON.parse(await fsp.readFile(manifestPath, 'utf8'));
const entry = manifest.items.find(x => x.bookId === selector.bookId && x.variant === selector.variant && x.profile === selector.profile);
if (!entry) throw new Error('fixture missing');
const payload = JSON.parse(await fsp.readFile(path.resolve(path.dirname(manifestPath), entry.payloadFile), 'utf8'));
if (payload.cover_url && !/^[a-z]+:/i.test(payload.cover_url) && !path.isAbsolute(payload.cover_url)) payload.cover_url = path.resolve(path.dirname(htmlPath), payload.cover_url);
const html = await fsp.readFile(htmlPath, 'utf8');
const match = html.match(/<script>([\s\S]*?)<\/script>/i);
if (!match) throw new Error('inline scene script not found');
const sceneSource = match[1];

function cpuUsec(){try{return Number(fs.readFileSync('/sys/fs/cgroup/cpu.stat','utf8').match(/^usage_usec\s+(\d+)/m)?.[1]||0)}catch{return 0}}
function cgroupMemory(){try{return Number(fs.readFileSync('/sys/fs/cgroup/memory.current','utf8').trim()||0)}catch{return 0}}
function processTreeRss(){try{const nodes=new Map();for(const n of fs.readdirSync('/proc')){if(!/^\d+$/.test(n))continue;try{const s=fs.readFileSync(`/proc/${n}/status`,'utf8');nodes.set(Number(n),{ppid:Number(s.match(/^PPid:\s+(\d+)/m)?.[1]||-1),rss:Number(s.match(/^VmRSS:\s+(\d+)\s+kB/m)?.[1]||0)*1024})}catch{}}const wanted=new Set([process.pid]);let changed=true;while(changed){changed=false;for(const[pid,v]of nodes)if(!wanted.has(pid)&&wanted.has(v.ppid)){wanted.add(pid);changed=true}}let sum=0;for(const pid of wanted)sum+=nodes.get(pid)?.rss||0;return sum}catch{return 0}}
function quantile(xs,p){const a=xs.filter(Number.isFinite).sort((x,y)=>x-y);return a[Math.min(a.length-1,Math.floor((a.length-1)*p))]||0}
function mean(xs){return xs.reduce((a,b)=>a+b,0)/Math.max(1,xs.length)}
function startResources(){const cpu0=cpuUsec();let rssPeak=processTreeRss(),cgPeak=cgroupMemory();const timer=setInterval(()=>{rssPeak=Math.max(rssPeak,processTreeRss());cgPeak=Math.max(cgPeak,cgroupMemory())},25);return()=>{clearInterval(timer);return{cpuMs:(cpuUsec()-cpu0)/1000,peakRssBytes:Math.max(rssPeak,processTreeRss()),peakCgroupBytes:Math.max(cgPeak,cgroupMemory())}}}

const scenePath='/examples/book-ad-systems/__r38_scene.html';
const server=http.createServer((req,res)=>{const u=new URL(req.url||'/','http://127.0.0.1');if(u.pathname===scenePath){const injected=html.replace('<script>',`<script>window.FRAMEWRIGHT_PAYLOAD=${JSON.stringify(payload)};<\/script>\n<script>`);res.writeHead(200,{'Content-Type':'text/html; charset=utf-8','Cache-Control':'no-store'});res.end(injected);return}const rel=decodeURIComponent(u.pathname).replace(/^\/+/,''),file=path.resolve(root,rel||'.');if(file!==root&&!file.startsWith(root+path.sep)){res.writeHead(403);res.end();return}fs.stat(file,(e,s)=>{if(e||!s.isFile()){res.writeHead(404);res.end();return}res.writeHead(200,{'Cache-Control':'no-store'});fs.createReadStream(file).pipe(res)})});
await new Promise((ok,no)=>{server.once('error',no);server.listen(0,'127.0.0.1',ok)});
const origin=`http://127.0.0.1:${server.address().port}`;
function sceneUrl(){const u=new URL(scenePath,origin);u.searchParams.set('f','0');u.searchParams.set('w',String(entry.width));u.searchParams.set('h',String(entry.height));u.searchParams.set('s',String(entry.seed));u.searchParams.set('profile',entry.profile);return u.href}

async function makeChromiumWorker(browser){const page=await browser.newPage();await page.goto(sceneUrl(),{waitUntil:'load',timeout:120000});await page.waitForFunction('window.__ready===true',{timeout:120000});return{async run(){return page.evaluate(async({width,seed,bitrate,queueLimit})=>{const canvas=document.getElementById('c'),rt=window.RISO,total=Number(rt.total),fps=Number(rt.fps||30);const chunks=[];let bytes=0,maxQueue=0;const enc=new VideoEncoder({output(c){bytes+=c.byteLength;chunks.push(c.byteLength)},error(e){throw e}});const cfg={codec:'avc1.420028',width:canvas.width,height:canvas.height,bitrate,framerate:fps,latencyMode:'realtime',avc:{format:'annexb'}};enc.configure(cfg);const t=performance.now();for(let f=0;f<total;f++){window.renderFrame(f,width,seed,canvas);const vf=new VideoFrame(canvas,{timestamp:Math.trunc(f*1e6/fps)});enc.encode(vf,{keyFrame:f===0||f%(fps*2)===0});vf.close();maxQueue=Math.max(maxQueue,enc.encodeQueueSize);while(enc.encodeQueueSize>queueLimit)await new Promise(ok=>enc.addEventListener('dequeue',ok,{once:true}))}await enc.flush();const ms=performance.now()-t;enc.close();return{ms,bytes,chunks:chunks.length,total,fps}}, {width:entry.width,seed:entry.seed,bitrate,queueLimit})},async close(){await page.close()}}}

async function makeNativeWorker(){const canvas=createCanvas(entry.width,entry.height);const document={createElement(n){if(String(n).toLowerCase()!=='canvas')throw new Error(`unsupported ${n}`);return createCanvas(1,1)},getElementById(id){return id==='c'?canvas:null}};const window={FRAMEWRIGHT_PAYLOAD:structuredClone(payload)},location={search:`?f=0&w=${entry.width}&h=${entry.height}&s=${entry.seed}&profile=${entry.profile}`};const sandbox={window,document,Image,URLSearchParams,location,console,performance,setTimeout,clearTimeout,requestAnimationFrame(){return 0}};sandbox.globalThis=sandbox;window.window=window;window.document=document;window.location=location;const ctx=vm.createContext(sandbox);vm.runInContext(sceneSource,ctx,{filename:htmlPath});const deadline=performance.now()+10000;while(!window.__ready&&performance.now()<deadline)await new Promise(r=>setTimeout(r,10));if(!window.__ready||window.__bootError)throw new Error(window.__bootError||'native boot timeout');vm.runInContext(`globalThis.__render=(n,w,s)=>{renderFrame(n,w,s,document.getElementById('c'));return document.getElementById('c');};`,ctx);return{async run(){const total=Number(window.RISO.total),fps=Number(window.RISO.fps||30),frameDur=Math.trunc(1e6/fps);let bytes=0,chunks=0,maxQueue=0,error=null;const enc=new NativeVideoEncoder({output(c){bytes+=c.byteLength;chunks++},error(e){error=e}});enc.configure({codec:'avc1.420028',width:canvas.width,height:canvas.height,bitrate,framerate:fps,latencyMode:'realtime',hardwareAcceleration:'prefer-software'});const t=performance.now();for(let f=0;f<total;f++){const rendered=ctx.__render(f,entry.width,entry.seed);const rgba=rendered.getContext('2d').getImageData(0,0,rendered.width,rendered.height).data;const vf=new NativeVideoFrame(new Uint8Array(rgba.buffer,rgba.byteOffset,rgba.byteLength),{format:'RGBA',codedWidth:rendered.width,codedHeight:rendered.height,timestamp:Math.trunc(f*1e6/fps),duration:frameDur});enc.encode(vf,{keyFrame:f===0||f%(fps*2)===0});vf.close();maxQueue=Math.max(maxQueue,enc.encodeQueueSize);if((f&15)===15)await new Promise(r=>setImmediate(r));if(error)throw error}await enc.flush();if(error)throw error;const ms=performance.now()-t;enc.close();return{ms,bytes,chunks,total,fps,maxQueue}},async close(){}}}

async function scenario(backend,concurrency){let browser=null;const resourcesStop=startResources();const t0=performance.now();const failures=[],rows=[];try{if(backend==='chromium')browser=await puppeteer.launch({headless:true,protocolTimeout:600000,args:['--no-sandbox','--disable-setuid-sandbox']});const workers=[];for(let i=0;i<concurrency;i++)workers.push(backend==='chromium'?await makeChromiumWorker(browser):await makeNativeWorker());const wall0=performance.now();await Promise.all(workers.map(async(w,idx)=>{for(let j=idx;j<jobsPerScenario;j+=concurrency){const s=performance.now();try{const r=await w.run();rows.push({job:j,wallMs:performance.now()-s,...r})}catch(e){failures.push({job:j,error:String(e?.stack||e)})}}}));const scenarioWallMs=performance.now()-wall0;for(const w of workers)await w.close();const resources=resourcesStop();const vals=rows.map(r=>r.wallMs);return{backend,concurrency,jobs:rows.length,failures,setupPlusScenarioMs:performance.now()-t0,scenarioWallMs,videosPerHour:rows.length*3600000/scenarioWallMs,p50JobMs:quantile(vals,.5),p95JobMs:quantile(vals,.95),meanEncodedBytes:mean(rows.map(r=>r.bytes)),meanEncodeMs:mean(rows.map(r=>r.ms)),resources:{...resources,cpuMsPerVideo:resources.cpuMs/Math.max(1,rows.length)},videosPerHourPerGiB:(rows.length*3600000/scenarioWallMs)/(resources.peakRssBytes/1073741824),rows}}finally{if(browser)await browser.close().catch(()=>{})}}

const results=[];try{for(const backend of ['chromium','native'])for(const c of concurrencies){console.log(`R38 ${backend} c${c}`);const r=await scenario(backend,c);results.push(r);console.log(JSON.stringify({backend,c,vph:r.videosPerHour,p50:r.p50JobMs,rss:r.resources.peakRssBytes,cpu:r.resources.cpuMsPerVideo,vphGiB:r.videosPerHourPerGiB,failures:r.failures.length},null,2))}const best={chromium:[...results].filter(x=>x.backend==='chromium'&&!x.failures.length).sort((a,b)=>b.videosPerHour-a.videosPerHour)[0],native:[...results].filter(x=>x.backend==='native'&&!x.failures.length).sort((a,b)=>b.videosPerHour-a.videosPerHour)[0]};const nativeMemoryFrontier=Math.max(...results.filter(x=>x.backend==='native'&&!x.failures.length).map(x=>x.videosPerHourPerGiB));const chromiumMemoryFrontier=Math.max(...results.filter(x=>x.backend==='chromium'&&!x.failures.length).map(x=>x.videosPerHourPerGiB));const decision={nativeThroughputRatio:best.native?.videosPerHour/best.chromium?.videosPerHour,nativeCapacityPerGiBRatio:nativeMemoryFrontier/chromiumMemoryFrontier,nativeEarnsDeepPass:Boolean(best.native&&best.chromium&&(best.native.videosPerHour>=best.chromium.videosPerHour*.9||nativeMemoryFrontier>=chromiumMemoryFrontier*1.25))};const report={schema:'framewright-r38-native-capacity-v1',fixture:{...selector,id:entry.id,width:entry.width,height:entry.height,seed:entry.seed},config:{jobsPerScenario,bitrate,concurrencies,scope:'warm scene-raster -> H264 encode only; no audio/mux/validation'},results,best,decision};await fsp.writeFile(reportPath,JSON.stringify(report,null,2)+'\n');const fmt=x=>x?`${x.videosPerHour.toFixed(1)} v/h, ${(x.resources.peakRssBytes/1073741824).toFixed(2)} GiB, ${x.videosPerHourPerGiB.toFixed(1)} v/h/GiB`:'n/a';const md=`# R38 native capacity scout\n\nChromium best: **${fmt(best.chromium)}**.\n\nNative best: **${fmt(best.native)}**.\n\nNative/Chromium best-throughput ratio: **${(decision.nativeThroughputRatio*100).toFixed(1)}%**. Native/Chromium memory-capacity frontier: **${(decision.nativeCapacityPerGiBRatio*100).toFixed(1)}%**.\n\nDecision: **${decision.nativeEarnsDeepPass?'native earns a deeper capacity/economics pass':'native remains fallback/reference; do not integrate as STANDARD capacity path'}**.\n`;await fsp.writeFile(path.join(outDir,'summary.md'),md);console.log(md)}finally{await new Promise(ok=>server.close(ok))}
