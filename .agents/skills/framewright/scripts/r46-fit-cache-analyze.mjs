#!/usr/bin/env node
import fs from 'node:fs/promises';
import path from 'node:path';

const basePath=path.resolve(process.env.BASELINE_REPORT||'artifacts/r46/baseline/report.json');
const cachedPath=path.resolve(process.env.CACHED_REPORT||'artifacts/r46/cached/report.json');
const parityPath=path.resolve(process.env.PARITY_REPORT||'artifacts/r46/parity.json');
const outPath=path.resolve(process.env.REPORT||'artifacts/r46/report.json');
const summaryPath=path.resolve(process.env.SUMMARY||'artifacts/r46/summary.md');
const [baseline,cached,crossParity]=await Promise.all([basePath,cachedPath,parityPath].map(async p=>JSON.parse(await fs.readFile(p,'utf8'))));
const profiles=['vertical','square','landscape'];
const pick=(r,p)=>r.results.find(x=>x.profile===p&&x.concurrency===2);
const delta=(candidate,control)=>control?candidate/control-1:0;
const q=(xs,p)=>{const a=[...xs].sort((x,y)=>x-y);return a[Math.min(a.length-1,Math.floor((a.length-1)*p))]||0};
function summarize(r){const rows=profiles.map(p=>pick(r,p));if(rows.some(x=>!x))throw new Error('missing c2 profile result');const all=rows.flatMap(x=>x.rows||[]),jobs=rows.reduce((a,x)=>a+x.jobs,0),wall=rows.reduce((a,x)=>a+x.scenarioWallMs,0);return{jobs,serialMixVideosPerHour:jobs*3600000/wall,p95JobMs:q(all.map(x=>x.wallMs),.95),cpuMsPerVideo:rows.reduce((a,x)=>a+x.cpuMsPerVideo*x.jobs,0)/jobs,drawMsPerVideo:all.reduce((a,x)=>a+x.drawMs,0)/Math.max(1,all.length),peakRssBytes:Math.max(...rows.map(x=>x.peakRssBytes)),h264BytesPerVideo:all.reduce((a,x)=>a+x.h264Bytes,0)/Math.max(1,all.length),failures:rows.reduce((a,x)=>a+x.failures.length,0),parity:rows.every(x=>x.preflightParity)}}
const b=summarize(baseline),c=summarize(cached);
const perProfile=Object.fromEntries(profiles.map(p=>{const x=pick(baseline,p),y=pick(cached,p);return[p,{baselineVph:x.videosPerHour,cachedVph:y.videosPerHour,throughputDelta:delta(y.videosPerHour,x.videosPerHour),baselineP95:x.p95JobMs,cachedP95:y.p95JobMs,p95Delta:delta(y.p95JobMs,x.p95JobMs),baselineRss:x.peakRssBytes,cachedRss:y.peakRssBytes,rssDelta:delta(y.peakRssBytes,x.peakRssBytes),baselineCpu:x.cpuMsPerVideo,cachedCpu:y.cpuMsPerVideo,cpuDelta:delta(y.cpuMsPerVideo,x.cpuMsPerVideo),parity:y.preflightParity,failures:y.failures.length}] }));
const aggregate={throughputDelta:delta(c.serialMixVideosPerHour,b.serialMixVideosPerHour),p95Delta:delta(c.p95JobMs,b.p95JobMs),cpuDelta:delta(c.cpuMsPerVideo,b.cpuMsPerVideo),drawDelta:delta(c.drawMsPerVideo,b.drawMsPerVideo),rssDelta:delta(c.peakRssBytes,b.peakRssBytes),bytesDelta:delta(c.h264BytesPerVideo,b.h264BytesPerVideo)};
const videosPerGiBBaseline=b.serialMixVideosPerHour/(b.peakRssBytes/2**30),videosPerGiBCached=c.serialMixVideosPerHour/(c.peakRssBytes/2**30);aggregate.videosPerGiBDelta=delta(videosPerGiBCached,videosPerGiBBaseline);
const correctness=crossParity.pass===true&&crossParity.cacheHealthy===true&&(crossParity.failures?.length||0)===0&&c.failures===0&&c.parity&&profiles.every(p=>perProfile[p].parity&&!perProfile[p].failures);
const benefitGate=aggregate.throughputDelta>=.05||aggregate.cpuDelta<=-.075||aggregate.drawDelta<=-.10;
const boundedCost=aggregate.p95Delta<=.05&&aggregate.rssDelta<=.10&&aggregate.videosPerGiBDelta>=-.02&&profiles.every(p=>perProfile[p].throughputDelta>=-.03&&perProfile[p].p95Delta<=.08&&perProfile[p].rssDelta<=.12);
const promoted=correctness&&benefitGate&&boundedCost;
const report={schema:'nightwill-r46-fit-cache-v1',candidate:'deterministic per-document fitBlock memoization keyed only by fit-affecting text/options',gate:{throughputGain:0.05,cpuReduction:-0.075,drawReduction:-0.10,p95Max:0.05,rssMax:0.10,videosPerGiBMin:-0.02,profileThroughputFloor:-0.03,profileP95Max:0.08,profileRssMax:0.12},crossParity:{samples:crossParity.samples,failures:crossParity.failures?.length||0,pass:crossParity.pass,cacheHealthy:crossParity.cacheHealthy,cacheRows:crossParity.cacheRows},baseline:b,cached:c,aggregate,perProfile,correctness,benefitGate,boundedCost,promoted,decision:promoted?'PROMOTE deterministic fit cache into canonical FAST renderer':'DO NOT ADOPT: current-base fit cache does not clear bounded value gate'};
await fs.mkdir(path.dirname(outPath),{recursive:true});await fs.writeFile(outPath,JSON.stringify(report,null,2)+'\n');
const pct=x=>(x*100).toFixed(1)+'%';const lines=['# R46 deterministic fit-cache adoption scout','',`Decision: **${report.decision}**`,'',`- sampled pixel parity: ${crossParity.pass?'PASS':'FAIL'} (${crossParity.samples} samples)`,`- cache exercised: ${crossParity.cacheHealthy?'PASS':'FAIL'} (all fixture probes hits > misses)`,`- mixed-profile throughput: ${b.serialMixVideosPerHour.toFixed(2)} -> ${c.serialMixVideosPerHour.toFixed(2)} videos/h (${pct(aggregate.throughputDelta)})`,`- p95 job: ${b.p95JobMs.toFixed(2)} -> ${c.p95JobMs.toFixed(2)} ms (${pct(aggregate.p95Delta)})`,`- draw/video: ${b.drawMsPerVideo.toFixed(2)} -> ${c.drawMsPerVideo.toFixed(2)} ms (${pct(aggregate.drawDelta)})`,`- CPU/video: ${b.cpuMsPerVideo.toFixed(2)} -> ${c.cpuMsPerVideo.toFixed(2)} ms (${pct(aggregate.cpuDelta)})`,`- peak RSS: ${(b.peakRssBytes/2**20).toFixed(1)} -> ${(c.peakRssBytes/2**20).toFixed(1)} MiB (${pct(aggregate.rssDelta)})`,`- videos/hour/GiB: ${videosPerGiBBaseline.toFixed(2)} -> ${videosPerGiBCached.toFixed(2)} (${pct(aggregate.videosPerGiBDelta)})`,`- correctness: ${correctness?'PASS':'FAIL'}; benefit gate: ${benefitGate?'PASS':'FAIL'}; bounded-cost gate: ${boundedCost?'PASS':'FAIL'}`,'',...profiles.map(p=>`- ${p}: throughput ${pct(perProfile[p].throughputDelta)}, CPU ${pct(perProfile[p].cpuDelta)}, p95 ${pct(perProfile[p].p95Delta)}, RSS ${pct(perProfile[p].rssDelta)}`),''];await fs.writeFile(summaryPath,lines.join('\n'));console.log(lines.join('\n'));
