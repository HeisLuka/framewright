#!/usr/bin/env node
import fs from 'node:fs';
import fsp from 'node:fs/promises';
import http from 'node:http';
import path from 'node:path';
import { spawn } from 'node:child_process';
import puppeteer from 'puppeteer';

const root=process.cwd();
const htmlPath=path.resolve(process.env.HTML||'examples/book-ad-systems/index-e18-for-r51.html');
const manifestPath=path.resolve(process.env.MANIFEST||'examples/book-ad-systems/generated-r51/manifest.json');
const outDir=path.resolve(process.env.OUT_DIR||'artifacts/r51');
const reportPath=path.resolve(process.env.REPORT||path.join(outDir,'report.json'));
const repeats=3, bitrate=3000000, queueLimit=8;
const DSSIM_FLOOR=-0.005, DPSNR_FLOOR=-1;
const ROI={hook:{frame:45,rect:{x:76,y:620,w:928,h:700}},book_cover:{frame:165,rect:{x:120,y:450,w:430,h:650}},book_title:{frame:165,rect:{x:570,y:540,w:434,h:470}},book_hook:{frame:195,rect:{x:490,y:1120,w:514,h:320}},cta_cover:{frame:285,rect:{x:350,y:500,w:380,h:560}},cta_title:{frame:300,rect:{x:60,y:1080,w:960,h:300}},cta_button:{frame:330,rect:{x:160,y:1340,w:760,h:220}}};
await fsp.mkdir(outDir,{recursive:true});for(const f of [htmlPath,manifestPath])if(!fs.existsSync(f))throw new Error(`missing ${f}`);
const manifest=JSON.parse(await fsp.readFile(manifestPath,'utf8'));if(manifest.schema!=='nightwill-r51-codec-acceptance-fixtures-v1')throw new Error('wrong manifest schema');
function run(cmd,args,{stdin}={}){return new Promise((ok,no)=>{const p=spawn(cmd,args,{cwd:root,stdio:['pipe','pipe','pipe']});let o='',e='';p.stdout.on('data',d=>o+=d);p.stderr.on('data',d=>e+=d);p.once('error',no);p.once('close',c=>c===0?ok({stdout:o,stderr:e}):no(new Error(`${cmd} exited ${c}\n${e.slice(-5000)}`)));if(stdin){(async()=>{try{for await(const b of stdin){if(!p.stdin.write(b))await new Promise(r=>p.stdin.once('drain',r));}p.stdin.end();}catch(err){p.stdin.destroy(err);}})();}else p.stdin.end();});}
const ctype=f=>({'.html':'text/html; charset=utf-8','.json':'application/json','.png':'image/png','.jpg':'image/jpeg','.webp':'image/webp'})[path.extname(f).toLowerCase()]||'application/octet-stream';
const uploads=new Map();
const server=http.createServer((req,res)=>{const u=new URL(req.url||'/','http://127.0.0.1');if(req.method==='POST'&&u.pathname.startsWith('/__r51_h264/')){const id=decodeURIComponent(u.pathname.slice('/__r51_h264/'.length)),a=[];req.on('data',d=>a.push(d));req.on('end',()=>{uploads.set(id,Buffer.concat(a));res.writeHead(204);res.end();});return;}const rel=decodeURIComponent(u.pathname).replace(/^\/+/,''),f=path.resolve(root,rel||'.');if(f!==root&&!f.startsWith(root+path.sep)){res.writeHead(403);res.end();return;}fs.stat(f,(err,st)=>{if(err||!st.isFile()){res.writeHead(404);res.end();return;}res.writeHead(200,{'Content-Type':ctype(f),'Cache-Control':'no-store'});fs.createReadStream(f).pipe(res);});});
await new Promise((ok,no)=>{server.once('error',no);server.listen(0,'127.0.0.1',ok)});const origin=`http://127.0.0.1:${server.address().port}`;
function clampRoi(r,w,h){const x=Math.max(0,Math.min(w-2,Math.round(r.x))),y=Math.max(0,Math.min(h-2,Math.round(r.y)));return{...r,x,y,w:Math.max(2,Math.min(w-x,Math.round(r.w))),h:Math.max(2,Math.min(h-y,Math.round(r.h)))};}
async function metric(kind,ref,cand,roi){const crop=`crop=${roi.w}:${roi.h}:${roi.x}:${roi.y}`;const filt=kind==='ssim'?`[0:v]${crop}[a];[1:v]${crop}[b];[a][b]ssim`:`[0:v]${crop}[a];[1:v]${crop}[b];[a][b]psnr`;const {stderr}=await run('ffmpeg',['-hide_banner','-loglevel','info','-i',ref,'-i',cand,'-lavfi',filt,'-f','null','-']);const re=kind==='ssim'?/All:([0-9.]+)/g:/average:([0-9.]+)/g;const m=[...stderr.matchAll(re)].at(-1);return m?Number(m[1]):null;}
async function decodeFrame(video,frame,out){await run('ffmpeg',['-hide_banner','-loglevel','error','-y','-i',video,'-vf',`select=eq(n\\,${frame})`,'-vsync','0','-frames:v','1',out]);}
async function contact(inputs,roi,out){const labels=inputs.map((_,i)=>`[${i}:v]crop=${roi.w}:${roi.h}:${roi.x}:${roi.y}[v${i}]`).join(';');const stack=inputs.map((_,i)=>`[v${i}]`).join('')+`hstack=inputs=${inputs.length}`;const args=['-hide_banner','-loglevel','error','-y'];for(const i of inputs)args.push('-i',i);args.push('-filter_complex',`${labels};${stack}`,'-frames:v','1',out);await run('ffmpeg',args);}

let browser;
try{
 browser=await puppeteer.launch({headless:true,protocolTimeout:600000,args:['--no-sandbox','--disable-setuid-sandbox']});const page=await browser.newPage();const fixtures=[];
 for(const entry of manifest.items){
  console.log(`R51 ${entry.set} ${entry.bookId}/${entry.style}`);const payload=JSON.parse(await fsp.readFile(path.resolve(path.dirname(manifestPath),entry.payloadFile),'utf8'));
  await page.evaluateOnNewDocument(p=>{window.FRAMEWRIGHT_PAYLOAD=p;},payload);const rel=path.relative(root,htmlPath).split(path.sep).map(encodeURIComponent).join('/');const url=new URL(`/${rel}`,origin);url.searchParams.set('f','0');url.searchParams.set('w',String(entry.width));url.searchParams.set('s',String(entry.seed));url.searchParams.set('profile',entry.profile);url.searchParams.set('book',entry.bookId);
  await page.goto(url.href,{waitUntil:'load',timeout:120000});await page.waitForFunction('window.__ready===true',{timeout:120000});const scene=await page.evaluate(()=>({total:Number(window.RISO.total),fps:Number(window.RISO.fps),width:document.getElementById('c').width,height:document.getElementById('c').height}));const rois=Object.fromEntries(Object.entries(ROI).map(([k,v])=>[k,{frame:v.frame,rect:clampRoi(v.rect,scene.width,scene.height)}]));const dir=path.join(outDir,entry.bookId);await fsp.mkdir(dir,{recursive:true});
  async function* pngFrames(){for(let f=0;f<scene.total;f++){const data=await page.evaluate(({f,w,s})=>{const c=document.getElementById('c');window.renderFrame(f,w,s,c);return c.toDataURL('image/png');},{f,w:entry.width,s:entry.seed});yield Buffer.from(data.split(',')[1],'base64');}}
  const x264=path.join(dir,'x264.mp4');await run('ffmpeg',['-hide_banner','-loglevel','error','-y','-f','image2pipe','-vcodec','png','-framerate',String(scene.fps),'-i','-','-c:v','libx264','-preset','veryfast','-crf','22','-pix_fmt','yuv420p','-an','-movflags','+faststart',x264],{stdin:pngFrames()});
  const refs={};for(const [name,r] of Object.entries(rois)){const canvas=path.join(dir,`canvas-${name}.png`);const data=await page.evaluate(({f,w,s})=>{const c=document.getElementById('c');window.renderFrame(f,w,s,c);return c.toDataURL('image/png');},{f:r.frame,w:entry.width,s:entry.seed});await fsp.writeFile(canvas,Buffer.from(data.split(',')[1],'base64'));const xd=path.join(dir,`x264-${name}.png`);await decodeFrame(x264,r.frame,xd);refs[name]={canvas,x264:xd,ssim:await metric('ssim',canvas,xd,r.rect),psnr:await metric('psnr',canvas,xd,r.rect)};}
  const runs=[];
  for(let rep=0;rep<repeats;rep++){const id=`${entry.bookId}-${rep}`;uploads.delete(id);await page.evaluate(async({id,width,seed,bitrate,queueLimit})=>{const c=document.getElementById('c'),total=Number(window.RISO.total),fps=Number(window.RISO.fps),chunks=[];const enc=new VideoEncoder({output(ch){const b=new Uint8Array(ch.byteLength);ch.copyTo(b);chunks.push(b);},error(e){throw e;}});enc.configure({codec:'avc1.420028',width:c.width,height:c.height,bitrate,framerate:fps,latencyMode:'realtime',avc:{format:'annexb'}});for(let f=0;f<total;f++){window.renderFrame(f,width,seed,c);const vf=new VideoFrame(c,{timestamp:Math.trunc(f*1e6/fps)});enc.encode(vf,{keyFrame:f===0||f%(fps*2)===0});vf.close();while(enc.encodeQueueSize>queueLimit)await new Promise(ok=>enc.addEventListener('dequeue',ok,{once:true}));}await enc.flush();enc.close();let n=0;for(const b of chunks)n+=b.length;const body=new Uint8Array(n);let o=0;for(const b of chunks){body.set(b,o);o+=b.length;}const resp=await fetch(`/__r51_h264/${encodeURIComponent(id)}`,{method:'POST',body});if(!resp.ok)throw new Error(`upload ${resp.status}`);},{id,width:entry.width,seed:entry.seed,bitrate,queueLimit});const raw=uploads.get(id);if(!raw)throw new Error(`missing ${id}`);const hp=path.join(dir,`wc-${rep}.h264`),mp=path.join(dir,`wc-${rep}.mp4`);await fsp.writeFile(hp,raw);await run('ffmpeg',['-hide_banner','-loglevel','error','-y','-fflags','+genpts','-r',String(scene.fps),'-i',hp,'-c:v','copy','-an','-movflags','+faststart',mp]);await fsp.rm(hp,{force:true});const metrics={};for(const [name,r] of Object.entries(rois)){const dec=path.join(dir,`wc-${rep}-${name}.png`);await decodeFrame(mp,r.frame,dec);const ssim=await metric('ssim',refs[name].canvas,dec,r.rect),psnr=await metric('psnr',refs[name].canvas,dec,r.rect),dSSIM=ssim-refs[name].ssim,dPSNR=psnr-refs[name].psnr;metrics[name]={ssim,psnr,dSSIM:+dSSIM.toFixed(6),dPSNR:+dPSNR.toFixed(3),insideBand:dSSIM>=DSSIM_FLOOR&&dPSNR>=DPSNR_FLOOR,decoded:dec};}runs.push({rep,bytes:fs.statSync(mp).size,metrics});}
  const failures=[];for(const [name,r] of Object.entries(rois)){for(const runRow of runs){const m=runRow.metrics[name];if(!m.insideBand)failures.push({roi:name,rep:runRow.rep,dSSIM:m.dSSIM,dPSNR:m.dPSNR});}if(runs.some(x=>!x.metrics[name].insideBand)){const out=path.join(dir,`contact-${name}.png`);await contact([refs[name].canvas,refs[name].x264,...runs.map(x=>x.metrics[name].decoded)],r.rect,out);}}
  for(const runRow of runs)for(const m of Object.values(runRow.metrics))delete m.decoded;
  fixtures.push({bookId:entry.bookId,style:entry.style,set:entry.set,pass:failures.length===0,failures,runs,referenceMetrics:Object.fromEntries(Object.entries(refs).map(([k,v])=>[k,{ssim:v.ssim,psnr:v.psnr}]))});
 }
 const calibration=fixtures.filter(x=>x.set==='calibration'),heldOut=fixtures.filter(x=>x.set==='heldout');const decision={candidate:'all 3 repeats x all 7 ROIs satisfy pre-existing R43 band: dSSIM>=-0.005 AND dPSNR>=-1dB',calibrationPass:calibration.every(x=>x.pass),heldOutPass:heldOut.every(x=>x.pass),heldOutAccepted:heldOut.filter(x=>x.pass).length,heldOutTotal:heldOut.length,promote:calibration.every(x=>x.pass)&&heldOut.every(x=>x.pass)};const report={schema:'nightwill-r51-replicated-codec-acceptance-v1',bitrate,repeats,thresholds:{dSSIMFloor:DSSIM_FLOOR,dPSNRFloor:DPSNR_FLOOR,source:'pre-existing R43 semantic equivalence diagnostic band'},manifest:{calibration:manifest.calibration,heldOut:manifest.heldOut},fixtures,decision};await fsp.writeFile(reportPath,JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(decision,null,2));for(const f of fixtures)console.log(`${f.set} ${f.bookId}/${f.style}: ${f.pass?'PASS':'FAIL'} ${f.failures.length?JSON.stringify(f.failures):''}`);
}finally{if(browser)await browser.close();await new Promise(ok=>server.close(ok));}
