name: R40 Explicit Color Conversion Scout

on:
  pull_request:
    paths:
      - '.agents/skills/framewright/scripts/r40-explicit-color-conversion.mjs'
      - '.agents/skills/framewright/scripts/r40-bt709-i420-control.mjs'
      - '.github/workflows/r40-explicit-color-conversion.yml'
      - 'docs/r40-explicit-color-conversion.md'
  workflow_dispatch:

jobs:
  explicit-color-conversion:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20.20.2
      - name: Install dependencies
        run: |
          npm install
          sudo apt-get update -qq
          sudo apt-get install -y ffmpeg fonts-dejavu-core
      - name: Syntax check
        run: |
          node --check .agents/skills/framewright/scripts/r40-explicit-color-conversion.mjs
          node --check .agents/skills/framewright/scripts/r40-bt709-i420-control.mjs
      - name: Run explicit conversion probe
        env:
          OUT_DIR: artifacts/r40
          REPORT: artifacts/r40/report.json
        run: node .agents/skills/framewright/scripts/r40-explicit-color-conversion.mjs
      - name: Run corrected BT.709 I420 control
        env:
          OUT_DIR: artifacts/r40
        run: node .agents/skills/framewright/scripts/r40-bt709-i420-control.mjs
      - name: Validate report completeness
        run: |
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          const r=JSON.parse(fs.readFileSync('artifacts/r40/report.json','utf8'));
          const c=JSON.parse(fs.readFileSync('artifacts/r40/bt709-i420-control.json','utf8'));
          if(r.schema!=='nightwill-r40-explicit-color-conversion-v1') throw new Error('wrong main schema');
          if(c.schema!=='nightwill-r40-corrected-bt709-i420-control-v1') throw new Error('wrong control schema');
          if(!r.rows?.['canvas-default']?.supported) throw new Error('default canvas path missing');
          for(const id of ['canvas-bt709-tv','raw-i420-bt709-tv']) if(!r.rows?.[id]) throw new Error(`missing ${id}`);
          if(typeof r.decision?.canvasOverrideSupported!=='boolean') throw new Error('missing decision');
          if(typeof c.decision?.syntheticPass!=='boolean') throw new Error('missing corrected control decision');
          console.log('main',JSON.stringify(r.decision,null,2));
          console.log('corrected-control',JSON.stringify(c.decision,null,2));
          NODE
      - name: Show result
        run: |
          cat artifacts/r40/summary.md
          cat artifacts/r40/bt709-i420-control.json
      - uses: actions/upload-artifact@v4
        with:
          name: r40-explicit-color-conversion
          path: |
            artifacts/r40/report.json
            artifacts/r40/bt709-i420-control.json
            artifacts/r40/summary.md
            artifacts/r40/*.mp4
          retention-days: 14
